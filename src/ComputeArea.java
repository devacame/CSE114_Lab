package src;
public class ComputeArea {
    public static void main(String[] args) {
        double radius, area;
        radius = 20;
        area = radius * radius * 3.14159;
        System.out.println("The area for a cicle of radius " + radius + " is " + area);
    }
}
